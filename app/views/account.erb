<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <h1>Welcome <%= @user1.username%></h1>
    <h3>Your Balance: <%= @user1.balance.to_f%></h3><br>

    <a href="index.erb">Log Out</a>
    <% if Helpers.is_logged_in?(session) %>
    <h1>Welcome <%= Helpers.current_user(session).username %></h1>
    <h3>Your Balance: <%= Helpers.current_user(session).balance %></h3>
    <h4><a href="/logout">Log Out</a></h4>

  </body>
</html>
