
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(id: "id") %>
    <h1>Welcome <%= Helpers.current_user(username: "username") %></h1>
    <h3>Your Balance: <%= Helpers.current_user(balance: "balance") %></h3>
    <a href="/logout">Log Out</a>
    <% else %>
    <a href="/">Home</a>
    <% end %>


  </body>
</html>
