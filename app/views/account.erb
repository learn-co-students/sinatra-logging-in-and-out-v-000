<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
    <h1>Welcome, <%= Helpers.current_user(session).username %></h1>
    <h2>Your Balance: <%= Helpers.current_user(session).balance %></h2>
    <h5><a href="/logout">Log Uut</a></h5>
  </body>
</html>
