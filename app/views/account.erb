<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if User.is_logged_in? %>
    <h1>Welcome <%= User.current_user.username %></h1>
    <h3>Account Balance: <%= User.current_user.balance %></h3>
    <a href="/logout">Log Out</a>
    <% else %>
    <a href="/">Home</a>


  </body>
</html>
