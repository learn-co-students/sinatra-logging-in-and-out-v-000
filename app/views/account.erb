<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    
    <% Helpers.is_logged_in?(session) %>
        "Welcome, <%= Helpers.current_user(session) %>"
        "Account Balance: <%= @user.balance %>"
        <a href="/logout">Log Out</a>
        <% else %>
        <a href="/error"></a>
        <% end %>
  </body>
</html>