<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <p> <h1>Welcome <%= Helpers.current_user(session).username %></h1> </p>
      
      <p> <h3>Your Balance: <%= Helpers.current_user(session).balance.to_f %></h3> </p>
      <a href="/logout"><h3>Log Out</h3></a>
    <% else %>
      <h5>You Must <a href="/">Log In</a> to View Your Balance</h5>
    <% end %>
    
  </body>
</html>