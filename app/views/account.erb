<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
  <% current current_user = Helpers.current_user(sesion) %>
  <% if current_user %>
    <%= current_user.username %>
    <%= current_user.balance %>
    <a href="/logout">Log Out</a>
  <% else %>
    <a href="/index">Home</a>
  <% end %>
  </body>
</html>