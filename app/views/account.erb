<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% User.self.is_logged_in?(@user) %>
        "Welcome, <%= @user.username %>"
         "Account Balance: <%= @user.balance %>"
         <a href="/logout">Log Out</a>
        <% else %>
        <a href="/error"></a>
        <% end %>
  </body>
</html>